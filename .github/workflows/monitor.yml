name: AI Website Monitor

on:
  schedule:
    # Run every 6 hours
    - cron: '0 */6 * * *'
  
  # Allows manual triggering from GitHub UI
  workflow_dispatch:

permissions:
  contents: write
  issues: write

jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Run monitor
        env:
          ASKSAGE_API_TOKEN: ${{ secrets.ASKSAGE_API_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: python monitor.py
      
      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: monitor-logs-${{ github.run_number }}
          path: monitor.log
          retention-days: 7
      
      - name: Commit storage changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add storage/
          git diff --quiet && git diff --staged --quiet || git commit -m "📦 Update handled releases [skip ci]"
          git push
